export type User = {
  username: string;
  email: string;
  userId: string;
  created: string;
  updated: string;
};

export type Article = {
  author: string;
  title: string;
  content: string;
  link: string;
};
